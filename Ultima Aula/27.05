<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <style>
        p{border: solid 2px blueviolet; }
    </style>


<div>
    <label for="txt-qntd"></label>
    <input type="number" id="txt-qntd">
    <input type="button" value="Enter">
</div>
<div id="div-paragrafos"></div>

    <script src="/arrays.js"></script>
</body>
</html>
---------------------------------------------------------
let numeros = [15,25,35];

console.log(`#0 : ${numeros[0]}`);
console.log(`#1 : ${numeros[1]}`);
console.log(`#2 : ${numeros[2]}`);
console.log(`#3 : ${numeros[3]}`);

/*
# SE
if(){
    CODIGO
}

#FUNCAO
function (){
    CODIGO
}

#ENQUANTO
while (){
    CODIGO
}

*/

// enquanto indice for menor ou igual 99

let idx=0;
let total = numeros.length;
while (idx < total){
console.log(`##${idx} : ${numeros[idx]}`);
idx = idx + 1;
}

//CRIAR UM LAÇO DE REPETIÇÃO 
//ESCREVER APENAS OS NÚMEROS PARES
//ENTRE 1 - 1000
//ESCREVER EM UM PARAGRAFO NO HTML
/* 
const divParagrafo = document.querySelector("#div-paragrafos")

let i = 0;//INICIO
while(i < 1000){//CONDIÇÃO
    if(i % 5==0){
    divParagrafo.innerHTML += `<p>${i}</p>`;
 }
    i++;//INCREMENTO
}
*/

//#PARA
for(inicio;condição;incremento){}

for(let i = 0;i < 1000;i++){
     if(i % 5==0){
    divParagrafo.innerHTML += `<p>${i}</p>`;
 }
}


//CRIAR UM CAMPO PARA DIGITAR QUANTIDA
//ESCREVER NA TELA A QUANTIDADE INFORMADA
function GerarParagrafos(){
    const div = document.querySelector("#div-paragrafos")
    const inptQtd = document.querySelector("#txt-qntd")
    const qntd = Number(inptQntd.value);
    dispatchEvent.innerHTML = "";

    for (let ad = 0; ad < qntd; ad++) {
        divParagraf.innerHTML += `<p>${ad}</p>`;
        
    }
}
