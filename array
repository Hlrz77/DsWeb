<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<div id="paragráfo"></div>

<body>
    <script src="/arrays.js"></script>
</body>
</html>
+------------------------------------------+
let numeros = [15,25,35];

console.log(`#0 : ${numeros[0]}`);
console.log(`#1 : ${numeros[1]}`);
console.log(`#2 : ${numeros[2]}`);
console.log(`#3 : ${numeros[3]}`);

/*
# SE
if(){
    CODIGO
}

#FUNCAO
function (){
    CODIGO
}

#ENQUANTO
while (){
    CODIGO
}

*/

// enquanto indice for menor ou igual 99
let idx=0;
let total = numeros.length; //define um limite como total
while (idx < total){
console.log(`##${idx} : ${numeros[idx]}`);
idx = idx + 1;
}

//CRIAR UM LAÇO DE REPETIÇÃO 
//ESCREVER APENAS OS NÚMEROS PARES
//ENTRE 1 - 1000
let asd = 0
while(asd <= 1000){
    if(asd%2==0){
    console.log (i);
    document.querySelector("#paragrafo").innerHTML="<p> esse numero e par </p>";
    }
    i++;
   
}

//ESCREVER EM UM PARAGRAFO NO HTML

